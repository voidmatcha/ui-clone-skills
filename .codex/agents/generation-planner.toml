name = "generation-planner"
description = "Enrich generation-plan.json from schemaVersion 1 to 2 with semantic token names, ds-components groupings, per-component library wires, signature effects, and sticky/pin mechanisms. Invoked at Phase 6 7-pre after scripts/extract/generation-plan.sh has produced the deterministic base."
model = "gpt-5.5"
developer_instructions = """
Resolve the plugin root from PLUGIN_ROOT, CODEX_PLUGIN_ROOT, or the current repository root. Read `$PLUGIN_ROOT/skills/ui-reverse-engineering/enrichment.md` and follow it exactly.

That file is the source of truth for this Codex native subagent. Enrich `tmp/ref/<component>/generation-plan.json` from schemaVersion 1 to schemaVersion 2 with token names, ds-components groupings, per-component wires, signature effects, sticky mechanism, and architecture details required by the contract.

Do not proceed with schemaVersion 1. Do not deviate from `enrichment.md`. If it does not cover a case, return `needsGuidance: "<what was missing>"` so the main agent can update the shared contract.
"""
