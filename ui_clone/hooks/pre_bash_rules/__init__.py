"""Rule modules for the PreToolUse Bash hook.

`pre_bash.py` orchestrates these rule families in `main()`. Each module
groups self-contained detector helpers / patterns so the top-level file
stays a thin dispatcher rather than a 1100-line monolith.
"""
